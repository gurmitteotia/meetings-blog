using System;
using MeetingBlog.OOP;
using NUnit.Framework;

namespace MeetingBlog.Tests.OOP
{
    [TestFixture]
    public class MeetingSchedulingTests
    {
        /// <summary>
        /// I will not write following test. 
        /// </summary>
        [Test]
        public void Schedule_meeting()
        {
            //var meetings = new[] {new Meeting("OOP", "org", DateTime.Today, DateTime.Now, DateTime.Now.AddHours(1))};
            //var calender = new CalenderStub();

            //Following can be an extension method on IEnumerable<Meeting> which is just looping over enumeration and scheduling meeting in calendar.
            //meetings.Schedule(calender);
        }
        
    }
}